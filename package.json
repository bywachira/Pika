{
  "name": "treda",
  "version": "1.0.0",
  "description": "HTML/CSS imaging service",
  "main": "index.js",
  "repository": "git@github.com:bywachira/Treda.git",
  "author": "Erick Wachira",
  "license": "GPL",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "api:build": "yarn --cwd packages/api build",
    "api:lint": "yarn --cwd packages/api lint",
    "api:start": "yarn --cwd packages/api start",
    "api:dev": "yarn --cwd packages/api start:dev",
    "test": "yarn --cwd packages/api test"
  }
}